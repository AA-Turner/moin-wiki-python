NumArray is the current reimplementation of [http://www.pfdubois.com/numpy/ NumPy].

Take a look at 
http://www.stsci.edu/resources/software_hardware/numarray
It is 100% backwards compatible with Num``Py (at least on the Python side - the C API is also very close though).
