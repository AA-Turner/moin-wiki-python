A MoinMoin:WikiBadge that indicates that the link that you wanted to follow is dead, so if someone else knows where the page has moved to can update the link accordingly.

Dead links are on these pages:
[[FullSearch(DeadLink)]]
